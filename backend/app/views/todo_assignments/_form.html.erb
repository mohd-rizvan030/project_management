<%= form_with(model: todo_assignment, local: true) do |form| %>
  <% if todo_assignment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(todo_assignment.errors.count, "error") %> prohibited this todo_assignment from being saved:</h2>

      <ul>
      <% todo_assignment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :todo_assignment_user_id %>
  </div>

  <div class="field">
    <%= form.label :todo_id %>
    <%= form.text_field :todo_id, id: :todo_assignment_todo_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
